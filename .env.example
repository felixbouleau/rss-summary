# Anthropic API Key (required)
ANTHROPIC_API_KEY="your_api_key_here"

# Path to the YAML file containing the list of RSS feeds (optional, defaults to feeds.yml)
# RSS_FEEDS_CONFIG="path/to/your/feeds.yml"

# System prompt for Claude AI (optional, defaults to "Summarize the provided content accurately and concisely.")
# CLAUDE_SYSTEM_PROMPT="Your custom system prompt here"

# Directory to store the generated RSS feed file (optional, defaults to ./rss)
# RSS_OUTPUT_DIR="./rss"

# Port for the HTTP server to serve the feed (optional, defaults to 8080)
# RSS_SERVER_PORT=8080

# Interval in seconds to refresh the feed summary (optional, defaults to 86400 - 24 hours)
# RSS_REFRESH_INTERVAL=86400
