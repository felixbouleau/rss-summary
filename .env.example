# --- Required Environment Variables ---

# Anthropic API Key (required by llm-anthropic plugin)
# llm will automatically pick this up if set.
# You can also store it using 'llm keys set anthropic'
ANTHROPIC_API_KEY="your_api_key_here"

# LLM Model ID (required, e.g., claude-3-5-sonnet, claude-3-haiku-20240307)
# Use 'llm models' to see available models after installing plugins.
# LLM_MODEL="claude-3-5-sonnet" # Optional, defaults to "claude-3.5-sonnet"

# Maximum tokens for the LLM response (optional, defaults to 4096)
# LLM_MAX_TOKENS=4096


# --- Optional Environment Variables ---

# Title for the generated RSS feed (optional, defaults to "AI Generated Feed Summary")
# RSS_FEED_TITLE="AI Generated Feed Summary"

# Path to the YAML file containing the list of RSS feeds (defaults to feeds.yml)
# RSS_FEEDS_CONFIG="path/to/your/feeds.yml"

# Directory to store the generated RSS feed file (defaults to ./rss)
# RSS_OUTPUT_DIR="./rss"

# Port for the HTTP server to serve the feed (optional, defaults to 8080)
# RSS_SERVER_PORT=8080

# Cron schedule string for refreshing the feed summary (optional, defaults to "0 9 * * *" - daily at 9 AM Local Time)
# Uses the system's local timezone where the script is run.
# See https://crontab.guru/ for help with cron syntax.
# RSS_CRON_SCHEDULE="0 9 * * *"

# Lookback window in hours for fetching feed entries (optional, defaults to 24)
# RSS_LOOKBACK_HOURS=24
